Create tables of DB

CREATE TABLE gym_instructors (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL
);

CREATE TABLE sessions (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    instructor_id BIGINT,
    session_type VARCHAR(255) NOT NULL,
    session_time VARCHAR(255) NOT NULL,
    FOREIGN KEY (instructor_id) REFERENCES gym_instructors(id)
);

CREATE TABLE participants (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    session_id BIGINT,
    FOREIGN KEY (session_id) REFERENCES sessions(id)
);



1. Create Instructor
Insert a new gym instructor:
INSERT INTO gym_instructors (name) VALUES ('Instructor Name');

Select all gym instructors:
SELECT * FROM gym_instructors;

**********Create session***************

insert into
  sessions (instructor_id, session_type, session_time)
values
  (
    (
      select
        id
      from
        gym_instructors
      where
        name = 'Yamin'
    ),
    'Zumba',
    'Afternoon'
  );
  
**********Create participants**********

insert into
  participants (name, session_id)
values
  (
    'angel',
    (
      select
        id
      from
        sessions
      where
        session_type = 'Zumba'
        and session_time = 'Afternoon'
    )
  )
  );